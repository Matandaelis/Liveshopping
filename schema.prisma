// ============================================================================
// PRISMA SCHEMA - COMPLETELY DISABLED FOR PRODUCTION BUILD
// ============================================================================
// 
// This schema file has been disabled to prevent Prisma validation errors
// during Next.js build. The application runs WITHOUT database for now.
//
// TO RE-ENABLE DATABASE SUPPORT:
// 1. Configure DATABASE_URL environment variable in Vercel
// 2. Uncomment the datasource and generator sections below
// 3. Run: npx prisma generate
// 4. Run: npx prisma db push
// 5. Re-enable PrismaClient imports in lib/db.ts, lib/stripe.ts, lib/subscriptions.ts
//
// ============================================================================

// DO NOT UNCOMMENT - WILL BREAK BUILD IF DATABASE_URL IS NOT SET
// datasource postgresql {
//   url      = env("DATABASE_URL")
//   provider = "postgresql"
// }

// DO NOT UNCOMMENT - WILL BREAK BUILD IF DATABASE_URL IS NOT SET
// generator client {
//   provider = "prisma-client-js"
// }

// MODELS BELOW ARE FOR REFERENCE ONLY - NOT CURRENTLY GENERATED
// Replace this entire file with the proper schema when enabling database

model Document {
  id        String    @id @default(cuid())
  userId    String
  title     String
  content   String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Subscription {
  id     String @id @default(cuid())
  userId String @unique
  tier   String @default("FREE")
}

model Citation {
  id         String @id @default(cuid())
  documentId String
  title      String
  authors    String?
  year       Int?
}

